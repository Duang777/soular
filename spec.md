# 思想银河产品规格

> 状态：持续维护
> 最近更新：2026-09-18
> 线上地址：<https://soular.top/>

## 1. 问题与目标

知乎热门问题通常包含数十至数百条回答。线性信息流适合阅读单篇内容，却不容易帮助用户快速理解讨论结构、发现相反观点，或确认自己在讨论中的位置。

思想银河把一个问题下的观点压缩成可探索的立场星图，让用户完成以下任务：

1. 快速看见讨论中的主要立场和分布。
2. 阅读代表性观点并回到知乎核验原文。
3. 通过点赞形成自己的观点坐标。
4. 发现相近、相反或处于中间位置的回答者。
5. 根据探索行为获得可分享的观点人格。

## 2. 产品原则

- **AI 位于内容生产侧**：AI 批量提炼观点、立场和人格标签，不为每位访客即时生成整片星云。
- **正式内容可验收**：每个公开星云必须经过来源、摘要、立场和素材检查后，以静态快照发布。
- **运行时轻量稳定**：浏览、筛选、点赞、匹配和人格计算优先在浏览器或普通服务端逻辑中完成。
- **原文可追溯**：真实观点必须保留知乎来源链接；摘要不能代替完整原文。
- **降级仍可使用**：动态接口不可用时保留静态星云、人格卡和知乎直达入口，不暴露上游技术错误。
- **配额有明确用途**：知乎数据接口用于发现候选内容，直答模型配额用于离线生产，不用于无限制用户聊天。

接口字段、缓存、鉴权和错误码以 [`docs/api-contract.md`](./docs/api-contract.md) 为准。

## 3. 核心体验

### 3.1 进入思想银河

用户从首页先进入漂浮问题场，选择已发布问题并在中央确认；点击生成后，静态快照在同一星空中经过汇聚、分离、成带和头像浮现，再进入可探索星云。人格卡是探索结果，不是首页主流程。

### 3.2 探索观点

用户可以旋转和缩放星云、悬停查看摘要、打开观点阅读流，并按昵称、人格或观点正文搜索当前快照。选择真实回答时可以打开知乎原文。

### 3.3 形成个人星位

用户点赞回答后，系统按已赞观点的平均立场移动个人星位。点赞数据按星云快照版本隔离，并保存在当前浏览器本地。

### 3.4 获得观点人格

用户至少完成 3 次不同回答的点赞后，系统根据点赞的人格分布与平均立场计算九派人格。同票时按立场接近度和固定顺序产生稳定结果，并生成可分享的人格卡与 3D 人格书。
已登录用户的人格卡同时展示知乎公开内容形成的兴趣关键词，但人格派别和本题立场仍只由当前问题中的点赞决定。

### 3.5 发现新话题

“搜索 / 推荐 / 热榜”在当前探索面板内工作：

- 搜索：输入问题或话题，在面板内查找已发布星云、回答者和观点摘要。
- 推荐：根据当前点赞立场推荐同频观点；尚未点赞时展示光谱两端和中点的代表观点。
- 热榜：读取当前知乎热门问题。
- 结果可打开知乎原文。
- 动态能力不可用时显示非技术性降级文案；热榜回退到已发布星云，并保留知乎直达链接。

这些入口不在访问时生成观点星云。

## 4. 功能状态

| 编号 | 功能 | 状态 | 发布状态 |
| --- | --- | --- | --- |
| F-001 | 问题场、静态观点星云与快照切换 | 已完成：7 个静态快照，入口云按数量椭圆排布 | 待发布更新 |
| F-002 | 观点阅读、点赞、星位与人格卡 | 已完成：含人格筛选与本题人格卡群 | 待发布更新 |
| F-003 | 搜索、推荐、热榜站内面板 | 已完成 | 已上线 |
| F-004 | 思想银河品牌、README 与 MIT 协议 | 已完成 | 已上线 |
| F-005 | 候选话题投票榜与离线生产流程 | 开发中：离线生产已完成 | 未发布 |
| F-006 | 星图向导轻量交互 | 已完成 | 未发布 |
| F-007 | 知乎 OAuth、首次导览与个性化画像 | 已完成：含登录后四步导览、前端画像、人格解释与匹配接入 | 已上线 |
| F-008 | 星云观测控制与键盘操作 | 已完成 | 待发布 |
| F-009 | 朋友独立表态、星位对照与共同思想地图 | 开发中：多题地图待验收 | 待发布 |
| F-010 | 距离分级渲染与头像局部降级 | 已完成 | 待发布 |
| F-011 | 主题人格库与首个九人格主题 | 已完成 | 待发布 |
| F-012 | 品牌落地页、Structure Flow 与 Soular 粒子字标 | 已完成 | 待发布 |
| F-013 | 知乎 × 思想银河开场动画 | 已完成：含会话门禁、跳过与减少动态降级 | 待发布 |

## 5. 功能规格

### F-001 静态观点星云

**用户故事**

1. 作为已登录用户，我希望快速看见一个问题下的观点分布，从而不必先读完所有回答。
2. 作为已登录用户，我希望切换不同问题的星云，从而持续探索新的讨论。
3. 作为内容维护者，我希望发布经过人工验收的快照，从而控制模型质量和线上成本。

**验收标准**

- 完成登录后，即使非登录类动态接口未配置，已发布星云仍能完整加载。
- 首页按“问题发现 → 中央确认 → 原地生成 → 星云探索”推进；生成动画不得调用知乎回答或 AI 接口。
- 确认页点击问题云朵只更新当前选择、标题和观点数，不得重新加载 iframe；选择不同题目后，
  仅在点击“凝聚观点星云”时加载一次目标场景并自动继续生成。
- 动态生成过程保持五阶段视觉叙事，完整动画预算为 3.2 秒；常规设备从点击生成到完整星云
  可交互应控制在约 4 秒内。
- 首页保留原有九派人格卡波浪舞台；已发布问题以搜索框和附近的悬挂问题签进入确认流程，不以问题场替换人格卡主视觉。
- 问题确认页必须提供顶部返回入口和次要返回按钮，两者都回到人格卡与问题搜索首页；直接打开确认链接时同样可返回。
- 生成后的探索态必须写入可恢复 URL；刷新、浏览器返回或切换快照后仍显示完整星云和“返回问题”入口。
- 每个真实快照包含问题、光谱语义、观点摘要、作者信息和来源链接。
- 光谱两端与中点的文案必须是该问题特有的具体主张；不得使用“支持、反对、质疑、中立”
  这类通用态度词，中点必须写清它在两端之间折中在哪。
- 离线生成为每条回答记录离轴度：完全在谈另一件事的回答不得与立场居中的回答同等呈现，
  模型未标注的回答也不得被当作中立立场。
- 快照人数可增长，不得在 React 导航或画像校验中使用未写入契约的固定人数上限。
- 快照回答增删、重排或换源时必须更新内容版本，旧点赞不得映射到新版回答。
- 快照文本只能作为纯文本渲染，原文入口只接受 HTTPS 知乎域名。
- 未知快照标识回退到默认快照。
- 星云初始化时彼此独立的功能模块与快照模块必须并发加载，不得随已发布快照数量形成
  串行网络瀑布。
- 星图向导、同频发现、观点碰撞和聚焦小圈子必须作为星云主界面常驻快捷入口，
  不得收纳在“知”面板内；移动端使用单行四列工具条。
- 桌面端和 390 px 移动端画布非空，交互控件不遮挡主要内容。

**验证记录**

- 2026-09-18：问题确认页的 7 朵题目云增加异步低频漂移和双层雾带流动；悬停或
  键盘聚焦时暂停，390 × 844 无横向溢出，减少动态模式关闭全部新增动画。
  本次仅完成本地开发验证，未部署。
- 2026-09-13：桌面浏览器完成问题选择、中央确认、完整生成、跳过生成和跨快照确认回归，控制台无错误或警告。
- 2026-09-13：390 px 视口确认入口与确认页无横向溢出，问题卡横向滚动，操作按钮纵向排列且可见触控控件不小于 44 px。
- 2026-09-13：首页恢复九派人格卡波浪舞台，在底部增加问题搜索与悬挂问题签，选择问题后复用确认、生成和星云探索流程。
- 2026-09-13：底部搜索框升级为黑金银河导航台，包含轨道星核、匹配星云数和低饱和金色进入动作；390 px 下搜索框与按钮均保持在视口内。
- 2026-09-13：确认页顶部“返回人格卡”和“返回问题搜索”均通过 iframe 消息回到来源首页；直接 URL 进入确认页时使用 `/` 兜底，返回后不会留下重复首页历史项。
- 2026-09-13：问题确认页使用稀疏星空与两朵可点击的雾化想法云；显示当前题序号与已发布总数，可在两座已发布星云之间直接切换，减少动态模式下星点保持静止。
- 2026-09-13：生成动画改为五阶段观点聚合过程：读取坐标、两侧汇入、建立立场轴、展开光谱、点亮回答者；背景与星云分层渐显，轨道仪表显示阶段刻度和百分比，保留 12 秒时长、跳过及减少动态直接完成。
- 2026-09-19：保留五阶段视觉时间轴，将实际生成等待从 8 秒压缩至 3.2 秒；人格书架与
  React 主题数据改为并行加载，抽卡洗牌、翻牌、进书架分别缩至 900、480、480 ms，
  朋友问卷防重复锁缩至 320 ms。
- 2026-09-19：真实浏览器确认生成完成后进入可探索态；主题人格页立即挂载带主题参数的
  书架 iframe，390 × 844 下页面宽度等于视口宽度且抽卡结果完整可见；朋友问卷在
  320 ms 防重复窗口后恢复选择，以上页面均无控制台错误或警告。
- 2026-09-13：生成态关闭背景模糊并降低 Bloom、点精灵尺寸、核心尘埃和头像前置光晕；中段画面保持冷暖双臂、中心核与离散星点边界清晰，完成后恢复正式星云亮度。
- 2026-09-13：观点卡片入口从银河工具箱中移出，改为星云右下角常驻的真实头像卡片叠层，直接显示回答者数量并打开光谱阅读；移动端与左侧人格卡入口并列且互不遮挡。
- 2026-09-13：银河工具箱三项发现能力可直接使用：搜索覆盖已发布问题、回答者与观点正文，推荐根据当前表态给出同频观点或光谱代表观点，热榜不可用时回退已发布星云；观点碰撞和小圈子保持三次表态门槛，但按钮始终可点击并提示剩余次数。
- 2026-09-13：从问题确认页生成的星云在顶部提供“返回问题”，可回到同一问题的确认态；生成完成后父 URL 同步为探索态，从人格卡浏览器返回不会误回确认页。开始生成前先同步绘制降亮度暗场，再揭开生成覆盖层并启动计时，避免完整星云短暂闪现。
- 2026-09-14：星图向导、同频发现、观点碰撞和聚焦小圈子从“知”面板移到星云主界面常驻工具条；
  1280 × 900 使用右侧纵向工具轨，390 × 844 与 320 × 720 使用顶部横向工具条，
  无圈子数据时其余三项自动等宽展开。各入口无文字溢出或相邻控件遮挡，弹层关闭后焦点返回对应入口。
- 2026-09-14：`scholars-ai-math` 通过 Access Secret 分页采集 47 条回答，并作为第 3 个问题入口发布。
- 2026-09-14：下线只有 5、3、3 个观点的 `tao-ai-tradition`、`pangdonglai-labor`
  和 `tao-ai-math-proof`；首页、快照注册、版本与人数目录只保留 3 个内容充足的星云，
  问题序号连续为 01、02、03。
- 2026-09-14：首页三个问题入口改为无卡片边框的“星轨坐标”：细金线连接三个节点，
  节点下只保留连续序号、问题标题和观点数；800、1280 和 1600 px 视口均完整同屏，
  720 px 以下继续沿星轨横向滑动。
- 2026-09-14：通过知乎问题回答 API 离线采集并人工验收“如何更好地社交”“AI 会导致
  程序员大批失业吗”和“大学毕业是去大城市好还是回小城市好”三个真实快照，每个快照
  包含 45 条去重回答摘要与知乎原文链接。初版因分页接口不提供作者、头像和赞同数字段，
  使用“知乎回答 NN”和通用本地头像；该降级方案已于 2026-09-18 移除。
- 2026-09-18：通过每条回答的公开详情补齐“如何更好地社交”“AI 会导致程序员大批失业吗”
  “大学毕业是去大城市好还是回小城市好”和遗产继承问题的真实作者姓名、头像与赞同数，
  共保留 148 条可验证回答。7 条已删除或失效回答因无法可靠绑定作者而从快照移除，四个快照
  分别保留 42、42、44、20 位回答者；头像下载为各快照独立的本地资源。
  新增 `check:preset-authors`，阻止真实快照再次使用编号名、人格素材或缺失头像；
  `build:preset` 在任一回答缺少真实姓名或头像时直接失败，不再生成占位身份。
- 2026-09-18：星云内点赞后的立场摘要只显示 5 秒，关闭按钮只收起摘要并保留点赞；
  观点卡片内点赞使用卡片状态反馈，不在返回星云后显示残留摘要。
- 2026-09-14：线上基线确认每次点击问题云朵都会重建 iframe，单次切换耗时 2732 ms；
  12 秒生成时间轴实测首张头像 8799 ms、完整星云 12807 ms。修复后连续切换三朵云分别
  为 0.2、0、0.1 ms，加载层不出现且 iframe `performance.timeOrigin` 不变；生成时间轴
  调整为 8 秒，同题首张头像约 6281 ms、完整星云 8303 ms。390 × 844 跨题生成只重载
  一次并在 8112 ms 完成，确认层、生成层与最终页面均无横向溢出。生成层完全隐藏后才
  恢复星云工具栏，1280 × 900 与 390 × 844 的 100% 收尾截图无控件叠盖，画布亮度范围
  分别为 `18..207` 和 `20..160`，均非空白。
- 2026-09-14：离线生成的模型输出改为每条只返回序号、立场、离轴度和九派，主张回退到
  摘要截取；`npm run server:check` 通过，新增检查覆盖“缺少主张字段仍计入标注”“离轴度
  按输出读取”“未标注条目记为 0”。真实热榜问题实测：加入标签约束后轴文案从
  “倾向质疑 / 审慎观察 / 倾向支持”变为  “旁系亲属应获继承 / 依法析产各有边界 /
  房产归国有法所当然”。已补测（见 2026-09-14 覆盖率与轴缺陷条目）：覆盖率从 8/23 提升到
  20/20，缩短输出有效。
- 2026-09-14：立场轴从单串标签拆成「主张 + 理由」两段。主张不超过 12 字说清认为什么，
  理由不超过 20 字说清凭什么；两者都必须加「我觉得」能读通，禁用态度词和圈内词。
  `ai-math` 改为“数学是在练人的思维 / AI 能用，但得有人复核 / AI 已经做出了真数学”，
  导语和「我」的引导语同步去掉“共同体、验证、加速”等词。理由渲染在三处：1280 px 星云
  极标第二行（逗号处强制断行）、观点卡片光谱轨两端、星图向导第三问选项副标题；
  390 px 星云极标隐藏理由，由卡片视图承载。未提供 `leftReason` 等字段的 preset 自动
  降级为单行，其余 4 个快照未改动。
  修复同批发现的既有缺陷：`.axis` 绝对定位未设 `width: max-content`，极标被夹到贴边时
  可用宽度只剩「包含块宽度 − left」，390 px 下整块塌成竖排一字一行。
  `npm run build` 与 `npm run server:check` 均通过，新增检查覆盖“模型返回理由时原样保留”
  “中间立场理由同样保留”“超长理由截断到 20 字并补省略号”“未返回理由时留空不编造”。
  实测 1280 × 900 与 390 × 844：两端极标完整显示、260 帧采样零溢出零重叠，卡片光谱轨、
  筛选条五行与向导三选项均无截断。已补测，并暴露两个缺陷，见下一条。
- 2026-09-14：换用新 Access Secret 后补测遗产继承问题（`--answers=20`，单次直答调用，
  耗时 7.1 秒）。**已验证**：覆盖率 20/20，`source: ai` 且无 warning，低离轴度 0 条，
  上一轮同题仅 8/23，缩短输出确实解决了覆盖率问题；两次条数不同（23 与 20），非严格对照。
  同时暴露两个缺陷：
  一、prompt 里「在前面加「我觉得」读起来要通顺」被模型当成输出指令，六个字段全部以
  「我觉得」开头，占掉 12 字预算中的 3 字，`center`、`right` 和 `leftReason` 被 `clip`
  截断；`right` 截在「亲戚来争遗产就是…」，主张本身丢失。
  二、prompt 只约束了「center 是两端的真实折中」，从未要求两端互相反对。实测 `left`
  （按法条亲戚没份）与 `right`（平时不照顾人走了才来抢）都在指责同一方，一个讲法律
  一个讲道德，两端不构成对立，立场分布因此偏成左 14 / 中 2 / 右 4。
  修复：prompt 改写为「心里默念「我觉得」是自检标准，输出里绝不能出现这三个字」，
  并新增「认同 left 的人一定不认同 right，两端若只是同一立场的两种说法就必须换轴」；
  模型原文经可选回调 `RawResponseSink` 落到 staging 的 `rawResponse` 字段，供人工核对
  截断前的原文，该字段不进入 `QuestionSpectrum` 类型，避免随快照发布外泄。
  `npm run server:check` 四项全通过。**验证受阻**：修复后的轴文案质量未实测。新 key 在
  第 3 次调用（20:10）返回 `rate_limit_exceeded`，此前仅成功 2 次（20:01 探针约 10 字、
  20:02 生成最多 1 万字）。限流口径存在两种未区分的解释——按请求体量计费，或配额按账号
  而非按 key 共享；区分二者需继续消耗配额，已主动停止探测。风险：修复能否同时消除
  「我觉得」前缀与两端不对立，尚无证据，不得据此发布新快照。
- 2026-09-14：离线生成管线打通，`heritage-state` 成为首个全程由脚本组装、未人工填写任何
  字段的快照。管线拆成两步，配额成本差一个量级：`prepare:question-preset` 烧 1 次直答，
  `build:preset` 只烧 1 次站内搜索且补全结果写回 staging，重跑组装零配额。
  新增 `ZhihuClient.zhihuSearch`：`question_answers` 分页不返回作者、头像和赞同数，只有
  站内搜索有。**实测上游硬上限**：`Count` 填 10、20、50 结果完全一致恒为 10 条，换三种
  query 写法命中数也完全一致，每题只有 2-3 条能与分页结果对上（`ContentToken` 等于
  answer id，可靠联结）。因此 `zhihuSearch` 不开放 `Count` 参数。
  初版把未命中搜索的回答降级为中性编号「知乎答主 NN」与自有九派素材；该方案随后被真实
  作者完整性要求替代。people 行第 8 位仍可逐条指定头像，`personAvatarPath` 优先读取该
  字段，缺失时沿用 `avatarBase` 旧路径。
  「我觉得」前缀改为在解析层强制剥离并置于 `clip` 之前：prompt 是请求不是保证，模型已
  证明会无视。新增检查覆盖「我觉得/我认为/个人觉得/我的看法是」四种前缀、前缀后带逗号，
  以及空理由剥离后仍为空串。
  修复同批发现的两个既有缺陷：一、`DEFAULT_CLUSTERS` 的成员下标按 48 人的 `career-35`
  写死，任何不带 `circles` 且人数少于 47 的快照都会在 `rankedCircles` 读到 `PEOPLE[43]`
  空洞并抛错，整个场景停在加载态；现按实际人数过滤越界成员，不足两人的圈子整体丢弃。
  二、`src/presetMeta.ts` 里 `ai-math` 的轴仍是改版前的「警惕生态伤害 / 审慎协作 /
  拥抱研究加速」，与 `preset-ai-math.js` 不一致，导致朋友对照视图与星云显示两套文案；
  导航检查只校验 id 不校验文案所以未拦住，现已同步。
  护栏补强：快照头像检查原本只在 `avatarBase` 存在时生效，新格式整个被跳过；现逐条校验
  第 8 位字段，只允许 zhimg.com 远程图或确实存在的本地素材。已反向验证——把 `goat` 改成
  `gaot` 会被拦下并报出具体位置。
  `npm run build` 七项检查与 `npm run server:check` 四项检查均通过。1024 px 实测：20 颗星
  全部渲染、九派头像正常、两端极标可读、观点卡片显示 20 位回答者、控制台零错误。
  **未验证**：390 px 移动视口未实测；轴文案仍带「我觉得」前缀且两端不对立，因为该快照的
  staging 数据是用修复前的 prompt 生成的，需直答配额恢复后重新生成，不得就此发布。
- 2026-09-14：长标题快照暴露的三处布局与数据缺陷，均由首个自动组装快照触发。
  一、`.journey-actions` 的 `top: max(92px, …)` 隐含「问题最多两行」，热榜标题普遍 40 字
  以上排到四行，实测问题块底 101px 压住工具轨顶 92px，重叠 9px。改为由脚本按问题块实际
  高度写入 `--head-bottom`，CSS 用 `max()` 取三者较大值；问题块在窄屏隐藏时高度为 0，
  自动回落到原下限。修复后留白 15px。
  二、极标理由的逗号强制换行是上一批引入的缺陷：理由允许 20 字、约 220px，必然超过 165px
  的极标宽度，第一段先自然折行、强制换行再切一刀，末尾剩下「了，」这种孤行。改回自然
  折行，由 `text-wrap: balance` 匀开行长，实测从 3 行含孤行变为 2 行。
  三、`leftChoice`/`rightChoice`/`leftTendency`/`rightTendency` 是人工快照才写的短标签，
  自动组装的快照没有；九处读取中只有星图向导做了回退，其余把 `undefined` 直接印到筛选条
  和卡片立场滑杆上。改为在 PRESET 入口统一归一化，缺失时回退到对应主张。
  新增离轴度渲染：people 行第 9 位携带 `relevance`，偏题回答画暗但不隐藏，悬停恢复原样。
  关键语义——`relevance: 0` 表示模型未标注而非判定偏题，因此不减亮度，否则会冤枉大多数
  回答并让不含该字段的既有快照整片变暗。
  回答摘要的可达性无需新增入口：观点卡片视图已完整展示摘要正文并提供「查看原回答」外链，
  星点悬停展示主张。
  `npm run build` 七项与 `npm run server:check` 四项均通过，1440 × 900 实测控制台零错误、
  页面无 `undefined` 文本。**未验证**：390 px 移动视口；本数据集最低离轴度仅 0.4，未覆盖
  prompt 定义的 `r < 0.3` 强偏题档，该档的观感需后续真实数据验证。
- 2026-09-14：离轴度的连续渐变改为两档。首版按离轴度线性插值透明度，实测看不出差别：
  离轴度 0.4 对应透明度 0.78，叠在近黑背景上物理亮度只降 22%，按暗场亮度感知的幂律
  折算约 8%，且 20 条里 18 条落在满亮度，渐变只制造了一堆互相分不出的中间值。
  改为单阈值两档，并同时动三个通道——透明度 0.38、尺寸 0.7、光晕近乎撤掉。
  悬停时三项全部还原，否则偏题星点会小到点不准、暗到看不清选中了谁。
  星点变暗必须给出理由，悬停卡片补「偏离这条轴」标签，避免被当成头像没加载完。
  **已验证**：1024 px 截图逐像素比对同款兔子头像，偏题的直径约为正常的 0.7、金环几乎
  熄灭、头像发灰，肉眼可直接分辨。平均亮度不能作为判据——头像自身底色差异更大
  （正常白兔 149 对正常企鹅 69.7），已改用尺寸与光环作为判据。
  `npm run build` 与 `npm run server:check` 通过。
- 2026-09-14：入口问题云改为按话题数量绕中心椭圆排布。原先 CSS 只写了
  `:nth-child(1..6)` 的绝对坐标，第 7 朵没有 top/left，塌到左上角与品牌区重叠；
  确认页长标题字号也过大。现由 JS 按可见数量写入 `--topic-x/y`，移动端仍走底部
  横向滑动（`position: static`），确认页标题改为最多三行并下调字号上限。
- 2026-09-14：跨题点「开始生成」时，「正在读取观点坐标」会播两遍。根因：旧页在
  导航前先 `setEntryGenerationCopy(0)`，新页 `generate=1` 又从 phase 0 再播。
  修复：跨题分支只发导航、不播生成文案；重载后先显示启动层「正在凝聚观点星云」，
  场景 looping 后再播生成叙事，同一句不会出现两次。同题生成不重载，路径不变。

### F-002 观点探索与人格

**用户故事**

1. 作为访客，我希望通过点赞表达认同，并看到自己的星位变化。
2. 作为访客，我希望找到与我最接近和距离最远的观点。
3. 作为访客，我希望获得稳定、可解释、可分享的观点人格。
4. 作为访客，我希望一次看到多位同频回答者，并理解彼此的相似点和不同点。
5. 作为访客，我希望按九派人格浏览本题观点，并看见该人格在光谱中的分布。

**验收标准**

- 点赞状态按快照版本保存在浏览器本地，刷新后可以恢复。
- 不足 3 次有效点赞时不得生成默认人格；第 3 次点赞后解锁人格与同频发现。
- 锁定的人格、同频、碰撞与圈子入口仍可点击，并直接打开观点阅读流；阅读流必须明确说明
  “点击「♡ 赞同」完成表态”，展示 0/3 到 3/3 的进度，并把焦点送到第一个未赞同按钮。
- 星位由已赞观点的平均立场决定；无点赞时使用快照默认值。
- 人格结果可解释，不把随机结果描述为用户真实画像。
- 同频和互补匹配以当前问题的立场距离为主要依据，知乎兴趣关键词只参与次级排序。
- 同频发现每批最多展示 5 位回答者，优先同人格且立场相近的候选，不足时按立场距离补齐；
  候选耗尽前不重复，最后一批不足 5 人时显示真实数量。
- 已看候选按快照版本和账号作用域保存在当前标签页；匿名状态独立，切换账号不得继承上一账号的轮次。
- 同频名单展示头像、昵称、本题观点、相似点、不同点和原回答入口；无来源链接或画像不可用时
  必须明确降级，不伪造链接或兴趣解释。
- 同频发现可从星云主界面常驻入口和已解锁的自我人格卡进入；取消点赞至不足 3 次后关闭名单并重新锁定。
- 从候选人格卡返回星云时恢复离开前的同频弹层与当前批次；仅在用户点击“换一批”后推进轮次。
- Local Storage 不可用时保留当前星云页内的三次表态体验，但人格卡不得展示无法恢复点赞状态的返回入口。
- 同频和互补各返回稳定排序的 3 位候选；当前候选必须展示立场分、兴趣交集和 82:18 权重，
  切换模式或候选不得随机抽取。
- 兴趣相关性优先使用完整关键词，其次使用英文 token 或中文双字片段；不得用单字重叠
  推断兴趣相关。
- 匹配弹层打开后焦点进入弹层，Tab 不得逃出；方向键可切换模式和候选，关闭后焦点返回
  可见的银河工具入口。
- 阅读流、筛选、搜索、原文跳转与返回星云均可用。
- 桌面端阅读流在观点列表、人格筛选栏和空白区域接收纵向滚轮时，均滚动同一观点列表；
  移动端继续使用原生触控滚动，不得出现内容超出可视区却无法滑动。
- 阅读流中的回答者观点整行只承担阅读和滚动，不得点击正文即进入对方人格卡；每行提供
  独立的“看卡片”按钮，鼠标、触控和键盘只能通过该按钮触发跳转。
- 点击回答者星点只显示观点浮层，不得直接进入人格卡；浮层正文只承担阅读，只有独立的
  圆形箭头按钮可以进入对方人格卡。“赞同”按钮保留独立按压反馈，评论浮层不提供箭头。
- 阅读流只展示当前快照中实际出现的人格筛选项；选择人格后仅过滤观点列表，并在星云中高亮同派星体、淡出而不移除其他星体。
- 主星云首屏常驻展示本题人格星群入口，用户无需先打开观点列表即可直接进入人格卡群。
- 星河尘埃、回答者头像、评论者、小圈子连线和两端标签必须共享同一旋转坐标系，拖拽或自动旋转后不得彼此漂移。
- 本题人格卡群支持在非空人格之间循环切换，显示该人格的回答者占比及最高赞代表观点；无赞同数时使用快照中的稳定顺序。
- 人格卡群提供“看看这一派怎么说”和“回到整体”，切换人格、返回星云与刷新均不得修改已保存的点赞。
- 星云到卡片页的临时画像按导航上下文隔离；公开分享链接不读取接收者的旧画像。
- 主题人格卡只等待轻量人格数据，不得等待 3D 书架完成初始化；星云与书架必须在加载
  Three.js 前启动主题解析，避免慢网下超时降级，或先显示“长答派”等旧人格再切换。
- 临时导航上下文最多保留 24 份，并按创建时间清理最旧记录。
- 人物或自我卡片返回其自身来源星云，不受同标签页后续切换的快照影响。
- 从星云打开人物或自我人格卡时保留当前星云 iframe；卡片覆盖期间暂停星云渲染，
  通过返回入口或浏览器后退时恢复同一实例，不重新加载快照和 Three.js 场景。
- Session Storage 不可用时，同页导航仍可通过路由状态打开真实人物、自我画像和通用人格卡。
- 快照升版后，旧自我卡分享链接仍可打开最终人格；依赖人物索引的旧版本链接必须回到对应星云。

**验证记录**

- 2026-09-13：从 `ai-math` 星云生成人格卡后，卡片路由和再次分享链接均保留 `preset` 与 `version`。
- 2026-09-13：快照契约检查确认所有已发布快照具有合法版本，版本化点赞键不超过存储限制。
- 2026-09-13：依次打开两个真实人物后返回前一个历史地址，人物卡仍按各自导航上下文恢复。
- 2026-09-13：公开自我人格卡链接不携带临时画像键，也不会复用当前标签页中的旧画像。
- 2026-09-13：禁用 Session Storage 后，`ai-math` 的真实人物和通用人格卡仍可加载。
- 2026-09-13：旧快照版本的自我卡链接仍可打开，旧人物索引链接继续拒绝错误映射。
- 2026-09-13：卡片返回目标由自身快照决定；非法人物来源链接会降级为对应观点星云。
- 2026-09-13：真实点击 3 个回答后才解锁人格、观点碰撞与同频发现；取消点赞降到 3 次以下会重新锁定并退出已有的同频名单和小圈子聚焦态。
- 2026-09-13：`ai-math` 真实快照按 5 人一批遍历 28 位未点赞候选，前 6 批无重复，末批如实展示 3 人，第 7 批才开启新一轮。
- 2026-09-13：`career-35` 无来源快照的五位候选均显示“暂无原回答”；画像不可用时仅解释本题人格和立场。
- 2026-09-13：Playwright 在 1280×900 与 390×844 下确认同频面板位于视口内、无横向溢出；移动端操作区高度不小于 44 px，Canvas 像素非空，Tab 焦点留在弹层并在关闭后返回可见入口。
- 2026-09-13：自我人格卡的“发现 5 位同频的人”可返回同一快照并自动打开名单。
- 2026-09-13：候选人格卡浏览器返回在 BFCache 与 iframe 重载路径下均恢复离开前批次，下一次“换一批”才继续未看候选。
- 2026-09-13：禁用 Local Storage 后人格卡不展示无法恢复状态的同频入口；模拟账号 A/B 切换确认各自浏览轮次隔离，切回 A 后继续下一批且不重复。
- 2026-09-13：连续切换 10 个身份并淘汰旧映射后，身份修订号仍单调唯一，旧账号轮次不会被新账号继承。
- 2026-09-13：`ai-math` 快照生成 9 个非空人格筛选项；人格卡群支持首尾循环，键盘切换后焦点保持在原方向按钮，同派星体高亮且其他星体仅淡出。
- 2026-09-13：桌面端刷新后版本化点赞仍保留；390 px 下人格卡群入口位于筛选首位，卡群无横向溢出，上一派、下一派、阅读与清除按钮均不小于 44 px。
- 2026-09-13：主星云新增外置人格星群入口；1280 px 下与观点入口纵向分组，390 px 下与底部操作区无重叠，点击后直接打开首个人格卡群。
- 2026-09-14：小圈子结果显示时隐藏右侧人格与观点常驻入口，关闭后恢复；1280 px、692 × 506 和 390 px 三档视口均无卡片重叠或横向溢出。
- 2026-09-14：修复星河尘埃独立旋转导致头像脱离旋臂；`1042 × 684` 实际拖拽后共享父节点保持一致，头像与尘埃参考点世界坐标误差为 0。
- 2026-09-14：小圈子聚焦会清除人格筛选；“看看这一派怎么说”会清除搜索与次级筛选。390 px 下打开卡群后焦点进入可见控件，关闭后回到原入口。
- 2026-09-14：减少动态模式下人格光圈固定为静态强度，两次间隔采样均为 `0.48`。
- 2026-09-14：与星云观测控制整合后，390 px 和 692 × 506 下人格入口、观测控制、银河工具箱与观点入口均无重叠。
- 2026-09-14：锁定的人格、同频与碰撞入口会打开观点阅读流并聚焦首个“♡ 赞同”按钮；
  0/3、1/3、2/3 到解锁状态均实测通过。1280、721、390 和 320 px 下指引、工具文案及
  76 px 表态按钮无裁切或横向溢出。
- 2026-09-14：修复主题人格页等待 3D 书架握手的问题；模拟 4G 冷缓存时，React 页面壳
  就绪后 204 ms 展示“尼采”人格卡，3D 书架后台完成且未出现“长答派”闪现。星云人格库
  在 199 ms 发起、Three.js 在 348 ms 发起，主题解析不再串行等待 3D 依赖。390 × 844
  下人格名称、加载状态与页面宽度均正常；本地星云到人格卡的端到端跳转为 450 ms。
- 2026-09-14：回答者观点取消整行跳转，只保留 44 px 高的“看卡片”按钮；桌面点击正文
  保持在星云，显式按钮进入人格卡。390 × 844 真实触控上滑 237 px 后仍停留在观点阅读流，
  按钮文字无裁切，页面与 iframe 均无横向溢出。
- 2026-09-14：桌面 1512 × 869 下，阅读区、人格栏和观点列表高度均为 728 px；滚轮位于
  人格栏或观点列表时均使观点列表滚动 100 px。390 × 844 触控滚动 237 px，页面及 iframe
  无横向溢出。
- 2026-09-14：线上返回星云基线为 4031 ms，确认瓶颈是两层串行动态导入；并发化后在
  250 ms 模拟网络延迟下重建耗时降至 1632 ms。人格卡改为背景路由后，桌面返回入口
  22 ms、390 × 844 浏览器后退 9 ms 恢复同一 iframe，`performance.timeOrigin` 保持不变；
  卡片覆盖期间星云旋转暂停，返回后恢复。1280 × 900 与 390 × 844 截图确认画布非空，
  移动端页面无横向溢出。
- 2026-09-14：桌面端回答者浮层实测 `pointer` 光标、冷青悬停描边、整卡 `0.975` 按压
  和“进入”位移反馈；“赞同”仅缩放自身至 `0.96`。390 × 844 触屏视口下浮层宽 248 px，
  页面无横向溢出且未触发桌面 hover 样式；真实点击“赞同”只增加点赞、不跳页，整卡点击
  仍进入对应人物页；评论浮层由静态门禁确认不获得人物入口状态。
- 2026-09-18：回答者浮层的人格区改为色点、弹性文案和圆形箭头三列布局；移除会挤压
  人格描述的“进入”文字按钮。桌面和 390 × 844 视口下长人格名与描述可自然换行，
  箭头保持 34 px 稳定尺寸，并使用 44 px 点击热区。
- 2026-09-18：取消星点头像和观点浮层整卡的跳转行为；鼠标或触控点击头像都只显示浮层，
  点击正文保持在星云，只有圆形箭头进入对应人格卡，赞同按钮仍只切换当前观点状态。

### F-003 站内发现面板

**用户故事**

1. 作为访客，我希望直接在星云面板中搜索已发布星云，减少无意义的页面跳转。
2. 作为访客，我希望查看相关推荐和热榜，再决定是否打开知乎原文。
3. 作为访客，我希望接口暂不可用时仍有明确下一步。

**行为**

- 搜索只查询本地快照目录、回答者和观点摘要，不请求上游；热榜在用户主动切换时请求。
- 服务端不提供公开搜索接口，任意关键词搜索通过知乎直达链接完成。
- 推荐不发起 API 请求，只在当前快照中按本题点赞立场选择同频观点。
- 结果在当前面板展示，选择具体条目后才打开知乎。
- 搜索直达链接携带当前关键词。
- 推荐直达知乎首页，热榜直达知乎热榜。
- 上游错误统一转化为非技术性降级文案，不显示“接口不存在”等技术信息。
- 热榜结果在浏览器内缓存 10 分钟；服务端固定缓存一份 `Limit=30` 的结果 6 小时并按请求条数切片。
  不同 `limit` 共用同一上游缓存键；同键请求复用浏览器在途 Promise，切换面板只忽略旧响应，
  不取消服务端可能共享的上游请求。

**验收标准**

- 搜索支持按钮与回车提交，可进入匹配的已发布星云。
- 搜索、推荐和热榜可以互相切换，并具有加载、成功、空结果和失败状态。
- 未找到星云或动态接口失败时，始终提供正确的知乎直达链接。
- 关闭结果面板后恢复观点碰撞、小圈子等原有工具。
- 不调用运行时 AI 观点光谱接口。
- 热榜前后端都只处理请求上限内的条目，异常上游响应不得创建无界 DOM。

**验证记录**

- 2026-09-13：`npm run build` 通过。
- 2026-09-13：`npm run check:navigation` 通过。
- 2026-09-13：桌面与 390 px 移动端浏览器回归通过。
- 2026-09-13：本地搜索 “AI” 命中已发布星云，知乎直达链接正确。
- 2026-09-13：390 px 视口下 WebGL 画布像素非空且无横向溢出。
- 2026-09-13：话题选择器打开时按 Escape 只关闭选择器，探索面板保持打开。
- 2026-09-13：390 px 视口下新增输入框、选择器、关闭按钮、提交按钮和直达链接均至少 44 px 高。
- 2026-09-13：快速切换热榜时同键请求只发起一次，结果最多渲染 8 条；10 分钟过期后重新请求。

### F-004 品牌与开源展示

**验收标准**

- 首页、观点星云、人格卡和人格书使用统一的思想银河标识。
- 品牌小标使用黑色底板、知乎蓝轨道和白色核心星点，浏览器 favicon 与站内标识保持一致。
- 开源仓库使用 `Duang777/soular`，静态镜像使用 `/soular/` 基础路径。
- README 展示真实产品截图、架构、运行方式、贡献边界和授权说明。
- 项目原创代码使用 MIT License；第三方素材和知乎内容不自动获得该授权。

**验证记录**

- 2026-09-13：favicon、README 横版 Logo、星云品牌小标和 React 品牌组件统一为黑底蓝轨道方案。
- 2026-09-13：桌面与 390 px 视口回归通过，品牌入口无横向溢出且移动端触控区域保持 44 × 44 px。
- 2026-09-13：根域与 `/soular/` 基础路径生产构建通过。

### F-005 候选话题投票榜

**状态：开发中；离线候选生成已完成，社区投票榜待开发**

**目标流程**

```text
知乎热榜 / 用户提名
        ↓
候选话题池
        ↓
社区投票排行榜
        ↓
运营选择待制作话题
        ↓
离线 AI 批量分析
        ↓
人工验收
        ↓
发布不可变静态快照
```

**边界**

- 用户投票决定候选优先级，不直接触发 AI 生成。
- 同一用户对同一候选只能保留一个有效投票状态。
- 排行榜应区分候选、制作中、审核中和已发布状态。
- 发布必须产生可追溯的快照版本和来源记录。
- 回答采集与观点光谱生成只由本地运营脚本执行，不提供匿名公网 API。
- 离线脚本按真实热榜问题采集有限页回答，批量生成观点光谱，并按立场从左到右排序。
- 候选文件使用临时文件原子替换；任一候选失败时保留原输出，且不会自动进入正式快照。
- 后续分页的普通瞬时错误保留已采集回答并记录警告；鉴权或配额错误仍须停止整批处理。
- 跨页重复回答按内容类型和内容标识去重，不得挤占有效回答数量。
- 热榜候选最多扫描上游返回的前 30 项，畸形或非问题链接不占候选数量。
- 模型输出解析或覆盖率不合格时允许保留摘要降级；模型调用、鉴权或配额错误必须停止整批处理。
- 任一候选生成失败后立即停止，避免继续消耗当日配额并覆盖已有候选文件。

**验证记录**

- 2026-09-13：`npm run server:check` 与服务端构建通过。
- 2026-09-13：`prepare:hot-spectrums --help` 可用；验证未读取密钥、未发起知乎或 AI 请求。
- 2026-09-13：畸形回答不占有效回答上限；回答分页或模型调用的配额错误都会向上抛出并停止候选生成。
- 2026-09-13：匿名 `/api/galaxy`、回答采集、光谱生成和搜索路由均返回 404。

### F-006 星图向导

**状态：已完成；待发布**

星图向导只解释和导航当前已发布快照，不负责生成新星云。

**首期能力**

- 用一句话概括当前争议。
- 推荐代表性、相反或中间观点。
- 根据点赞解释用户当前立场和人格结果。
- 通过三道选择题帮助未点赞用户定位起始星位。
- 已有 1–2 次点赞时只解释当前立场，达到 3 次后才展示本题人格结论。

**实现约束**

- 常见交互使用快照中的预生成摘要和本地计算。
- 自由输入先做意图识别，再匹配有限的可回答范围。
- 无法回答时提供可选问题，不伪造观点或来源。
- 首期不开放无限制通用聊天，不为每次对话调用大模型。
- 后续小模型兜底必须具备全站缓存、单用户限额和每日熔断。
- 向导打开后焦点进入对话框并限制在其中，关闭后回到主界面的星图向导入口。
- 中文输入法确认候选词不得触发提交；移动端可见操作区域不小于 44 px。

**验证记录**

- 2026-09-13：导航契约检查覆盖向导入口、选择题按钮挂载、代表观点意图、IME 组合态、焦点管理、触控区域和圈子提示恢复。
- 2026-09-13：桌面与 390 px 浏览器回归通过；三题流程、代表观点、1 次点赞门槛、焦点循环、Escape 返回和 44 px 触控区域均正常，WebGL 画布采样非空。

### F-007 OAuth 与个性化画像

**状态：已完成；已上线**

- 登录只通过顶层导航进入知乎授权页。
- 正式站与生产镜像允许未登录用户浏览首页；用户点击问题、人格卡或访问其他产品路由时，
  先弹出登录引导，既可授权后进入，也可关闭提示后在当前标签页匿名进入。
- 平台未返回 OAuth `state` 时，仅允许同一 Session 在 10 分钟内已发起的授权继续，并明确显示
  “仅适合临时联调”；不得将该降级路径描述为已完成生产级 `state` 校验。
- GitHub Pages 不尝试跨站读取登录 Cookie，登录操作仍前往正式站；关闭提示则保留本地匿名体验。
  开发模式保留本地调试。
- 浏览器不接收 App Key、Access Secret、OAuth Token 或原始 Session。
- 未登录、未配置 OAuth 或登录状态不可确认时，生产环境先展示登录提示；关闭提示后允许当前标签页匿名浏览静态星云、匹配和人格流程。
- 用户资料读取失败时保留授权状态，不伪造昵称或头像。
- 首页展示登录状态和退出入口；观点星云的“我”节点使用已登录用户的知乎头像，
  头像不可用时回退本地占位图。
- 已登录用户进入首页或星云时读取画像；首页账号状态展示星谱校准结果并可再次打开详情。
- OAuth 成功回跳后自动打开“知乎兴趣星谱”校准台；接口返回前只显示真实等待状态，
  不展示模拟百分比、虚构阶段或推测关键词。
- 同一账号在当前浏览器首次登录并结束兴趣星谱校准后，必须在真实首页上逐项高亮账号星谱、
  人格卡、问题卡和进入银河入口；支持上一步、下一步与跳过，完成后不再自动弹出。
- 完成首页导览后，用户首次进入实际星云探索态时必须自动接续现有星图向导；跳过首页导览时
  不自动打开，确认选题页也不得提前遮挡。
- 校准完成后展示创作、关注、收藏夹、近期收藏四项汇总数量，以及最多六个兴趣关键词的
  相对强度；刘看山和知乎蓝只承担来源识别，不改变现有黑金银河视觉体系。
- 校准台必须明确说明原始收藏与关注明细不会进入星云；失败时提供重试和关闭路径。
- OAuth 状态查询只读取 Session；头像缺失时通过独立资料接口补取，不阻塞登录状态。
- React 仅将最多 6 个兴趣关键词、分数、证据总数和部分数据标记发送给星云 iframe，
  不传递创作、关注、收藏明细或任何 Token。
- 本题人格派别和立场仍由当前星云内至少 3 次点赞决定；画像只作为人格卡上的兴趣底色，
  不把跨话题兴趣解释为当前问题立场。
- 观点匹配同时支持同频和互补模式，排序权重固定为本题立场 82%、兴趣相关性 18%。
- 观点碰撞展示 3 位稳定候选及当前候选的双依据；银河工具箱“相关推荐”复用同频排序，
  显示同频分与命中的兴趣词，不调用新的推荐接口。
- 画像只进入当前标签页的临时人格上下文；公开分享 URL 不包含关键词或画像统计。
- 资料与画像响应必须携带同一账号版本；标签重新可见时重新校验，版本不一致的结果丢弃。
- 临时人格画像必须绑定账号版本；退出或切换账号时立即清理旧头像、画像、碰撞结果和圈子聚焦。
- 画像加载失败时保留登录头像、本地表态、人格卡和无画像匹配流程。
- 匿名状态查询不创建 Session；画像接口不提供公开缓存绕过参数。
- 资料和画像的并发请求必须共享缓存发布过程；KV 读写失败不得覆盖成功的上游结果。
- 必须回归授权、回调、过期、退出和画像降级路径。

**验证记录**

- 2026-09-18：从 `main` 发布 Cloudflare Worker 版本
  `2db0fdbd-f2dc-4f79-8e03-171d56e4eedb`；正式站匿名状态确认首页可浏览，问题、
  人格、星云、书架、匹配和确认入口均回到登录引导，且受保护内容未挂载。
- 2026-09-15：首页模块聚光式四步导览通过构建、静态回归及桌面与 390 × 844 浏览器检查；
  完成或跳过均按账号记录，重复登录不再自动弹出。
- 2026-09-15：首页导览完成后会在首次进入星云探索态时接续星图向导；确认页不提前打开，
  跳过首页导览不触发星云向导。
- 2026-09-15：在包含 7 个问题入口的最新 `main` 上验证延迟登录门槛；未登录可浏览首页，
  点击问题或人格卡后弹出登录引导，星云、匹配页和人格书直链仍不可绕过。
- 2026-09-19：登录提示右上角关闭按钮可立即进入当前匿名流程，无网时无需等待 OAuth 状态；
  星云“我的星位”浮层可正常打开本题人格卡。
- 2026-09-13：安装项目级知乎 Hackathon Skill 与官方 `zhihu` Skill，CLI 版本兼容。
- 2026-09-13：OAuth 配置、授权跳转、Token 交换、双凭证用户资料请求和 Session 状态通过离线回归。
- 2026-09-13：正式站 OAuth 状态为已配置，授权入口正确跳转知乎登录页并携带登记回调。
- 2026-09-13：用户在正式站完成真实授权，首页正确显示连接状态及退出入口。
- 2026-09-13：用户生成未缓存画像后返回 `ok: true`、`cached: false` 且 `warnings` 为空；创作、关注、收藏夹、收藏夹内容和近期收藏五项接口完成真实验收。
- 2026-09-13：本地回归覆盖 OAuth 流程原子认领、旧回调隔离、退出竞态、会话续期 alarm、请求超时和失败重试。
- 2026-09-13：桌面与 390 px 浏览器回归确认登录头像可替换观点星云和卡片视图中的“我”占位图，金色身份徽章保留且旧直线尾迹已移除。
- 2026-09-13：使用固定画像完成 1280 px 与 390 px 浏览器回归；同频/互补切换、兴趣参与排序、临时上下文传递和人格卡兴趣底色均通过。
- 2026-09-13：390 px 下匹配弹层位于视口内，模式按钮高度不小于 44 px；人格卡兴趣词与本题解释完整显示且无横向溢出。
- 2026-09-13：用户在画像请求完成前打开自我人格卡时不阻塞导航，卡片页在后台补齐兴趣信号；禁用 Session Storage 时仍使用当前标签页内存上下文，退出登录后浏览器历史不再恢复旧人格数据，画像超时或失败则保留无画像流程。
- 2026-09-13：针对已授权但 `profile:null` 的真实状态补充回归，兼容 `/user` 顶层 `fullname/avatar_path`，现有 Session 可从独立资料接口按 Token 指纹缓存补取公开资料，无需重新授权。
- 2026-09-13：真实 Session 在字段兼容上线后仍返回 `profile:null`，增加仅针对明确鉴权失败的 OAuth Bearer 回退，并完成先失败、后通过的回归验证。
- 2026-09-13：资料补取使用 6 秒统一预算、60 秒空结果退避和非空字段合并；状态、资料与画像通过账号版本校验，多标签切号后不会组合旧头像与新画像。
- 2026-09-14：按黑客松官方基础资料契约将 `/user` 改为单次 OAuth Bearer 请求，不再误用数据接口的 Access Secret 双凭证；资料缓存升级为 `oauth-profile:v2`，使旧空结果不会继续隐藏登录头像。
- 2026-09-13：探索态刷新与切换快照保留可返回状态；人格卡重新可见时复查账号版本，切号或退出后同步移除旧头像、兴趣画像、碰撞和圈子聚焦。
- 2026-09-13：资料与画像 in-flight 合并覆盖缓存发布阶段；KV 读取或写入异常时仍返回可用的上游结果。
- 2026-09-13：OAuth 成功回跳自动打开兴趣星谱；真实等待态不展示关键词，结果态展示四项汇总和五个固定测试关键词，账号入口可再次打开。
- 2026-09-13：画像首次失败后可在校准台重试并恢复；1280 px、390 px、320 px 下无横向溢出，关闭按钮和主操作高度均为 44 px。
- 2026-09-13：固定画像下同频候选稳定为同一顺序，选择第二位与“换一位”依次切换到第 2/3、3/3 位；互补模式返回独立排序。
- 2026-09-13：银河工具箱相关推荐返回 5 个同频观点并展示兴趣命中；1280 px、390 px、320 px 匹配弹层无横向溢出，320 px 标题无孤字。
- 2026-09-13：键盘回归覆盖打开聚焦、模式切换、候选切换、Tab 焦点锁定与关闭恢复；
  “数学建模”可匹配数学内容，“烘焙甜点”在当前快照中的兴趣相关分为 0。

### F-008 星云观测控制

**状态：已完成；待发布**

**用户故事**

1. 作为访客，我希望随机遇见一个尚未浏览的回答者，并直接看到其观点。
2. 作为访客，我希望只看当前快照标记的关注对象，或快速回到自己的星位。
3. 作为键盘用户，我希望无需拖拽和滚轮也能旋转、缩放和重置星云。

**验收标准**

- 随机发现只选择回答者，不选择“我”或评论者；候选不少于两人时不得连续命中同一人。
- 随机发现与“我的位置”会平滑定位相机并打开对应观点浮层；仅在减少动态效果时直接完成定位。
- “只看关注”淡出未关注回答者，保留完整光谱与“我”；快照没有关注数据时给出明确提示。
- 重置视角恢复默认相机、全部回答者、默认评论状态、现有聚焦筛选和缓慢自动旋转，不清除点赞。
- 方向键旋转与加减号缩放按帧加速和减速；空格随机发现、`R` 重置和 `Escape` 关闭均可用，且不抢占输入框或按钮事件。
- 桌面端与 390 px 移动端均提供不小于 44 px 的等价操作按钮，无横向溢出或主要内容遮挡。
- 全部行为只读取当前静态快照，不调用知乎或 AI 接口，不改变 Three.js 最终坐标算法。

**验证记录**

- 2026-09-13：`npm run build` 与 `npm run check:navigation` 通过。
- 2026-09-13：桌面端验证四项控制、随机排重、用户星位定位、方向键旋转、加减号缩放和重置视角。
- 2026-09-13：390 px 视口下四个按钮均为 44 × 44 px，与底部入口无重叠且页面无横向溢出；WebGL 画布像素非空。
- 2026-09-13：无关注数据时显示降级提示；搜索输入框聚焦时，空格与 `R` 不触发星云快捷键。
- 2026-09-13：键盘相机改为按帧时间阻尼运动，60 fps 下首帧渐进、最大单帧旋转小于 0.014 弧度，松键后平滑衰减。
- 2026-09-13：画布可通过键盘聚焦，键盘定位后观点区域接收焦点；减少动态模式下定位高亮保持静止。
- 2026-09-13：390 px 下小圈子提示会避开观测控制条；切换关注筛选或打开弹层会清理不再可见的定位状态。

### F-009 朋友独立表态与星位对照

**状态：开发中；多题共同思想地图待验收**

- 自我人格卡分享面板提供对照链接，公开载荷最多包含 6 个已完成问题的快照 id、版本、人格和星位。
- 分享端只汇总当前账号已完成至少 3 次表态且版本仍有效的问题；账号作用域彼此隔离。
- 对照链接中的分享信息未经平台身份认证，页面必须明确提示，不把链接参数描述为可信身份声明。
- 朋友打开链接后先完成 3 次本地表态，完成前页面不直接展示分享者的星位。
- 完成后在同一光谱展示双方星位，并用本地规则生成关系描述，不调用 AI。
- 单题揭晓后可继续回答分享载荷中的其他共同问题；答案只保存在当前 React 页面状态，
  不写入接收者的 Local Storage 或 Session Storage。
- 共同思想地图只累计双方都已回答的问题，并按题展示双方星位、距离、坐标接近题数和
  差异明显题数；仅分享者已回答的问题标记为“等你回答”，不计入地图摘要，已完成问题
  可返回查看单题关系。
- 对照页采用冷白纸面星轨视觉：朱红表示朋友与当前行动，墨绿表示接收者；邀请首屏
  直接展示问题轨道，不使用大面积暗色卡片、满宽胶囊按钮或装饰性渐变。
- 缺失、畸形或未知参数直接返回首页；旧快照版本显示明确的过期状态，不使用当前光谱解释旧坐标。
- 公开对照链接不读取分享者或接收者的 Session Storage，也不包含知乎画像、昵称或 Token。
- 双方坐标重合或接近时，标记仍须分别可见；短屏和移动端页面必须可滚动。

**验证记录**

- 2026-09-13：分享对照检查覆盖链接编解码、缺失立场、未知快照、旧版本、中立关系文案和标记错层。
- 2026-09-13：桌面与 390 × 480 浏览器回归通过；合法链接三次表态后揭晓，非法参数返回首页，旧版本显示过期状态，短屏可滚动到底且双方标记保持可见。
- 2026-09-14：三题公开链接在桌面端连续完成 9 次独立表态，揭晓前分享者标记为 0，
  每题揭晓后显示 2 个错层星位，最终地图包含 3 行和 6 个星位；公开页 Storage 读取为 0。
- 2026-09-14：390 × 844 下完成 1 题后，地图显示 1 行已完成和 2 个 44 px 待回答入口，
  三列摘要等宽且无横向溢出。旧单题链接继续显示 1 道共同问题，次要快照过期时只移除
  失效题目并明确提示，不影响有效题目。
- 2026-09-14：分享端依次在 `career-35` 与 `ai-math` 完成表态后，本地账号目录保存 2 个
  当前版本坐标；生成的对照链接以当前题为主项，并附带另一题的紧凑地图载荷。
- 2026-09-14：纸面星轨视觉在 1280 × 900、375 × 844 与 320 × 700 下通过验收；邀请、
  答题、双星揭晓、累计地图和旧链接过期五种状态均无横向溢出。375 px 邀请页标题固定
  两行，主操作 147 × 46 px；320 px 下页面可纵向滚动且导航栏无溢出。

### F-010 距离分级渲染与头像局部降级

**状态：已完成；待发布**

- 相机距离使用带滞回的近、中、远三级 LOD，避免临界距离反复切换。
- 每位回答者只保留当前档位的一张本地 Canvas 纹理，并按距离在 256、128、64 px 间同步替换且释放旧纹理；拉近恢复清晰度，切换不重新请求资源。
- 粒子 draw range 和 Bloom 随距离收敛，同时保留光谱主体、回答者头像和现有设备质量档位。
- 单个头像失败或 8 秒无响应时保留固定尺寸的本地首字占位，不进入整页错误状态，不影响旋转、缩放或其他观点。
- 星云观点浮层和观点阅读卡均提供单头像重试；重试期间不改变节点或卡片布局。
- 减少动态效果继续生效，桌面与 390 px 移动端不得出现横向溢出或空白画布。

**验证记录**

- 2026-09-14：`npm run build` 与 `npm run check:lod` 通过，三级阈值、滞回、头像分辨率和粒子预算具有回归检查。
- 2026-09-14：同一 `ai-math` 快照在 full 设备档位下，近景为 26,600 粒子和 256 px 头像，远景为 13,032 粒子和 64 px 头像，粒子提交量降低 51.0%。
- 2026-09-14：Playwright 在 1280 × 900 与 390 × 844 下确认近景、远景 Canvas 像素非空，无横向溢出和页面异常。
- 2026-09-14：拦截单个头像后，星云保持可操作且全页错误层关闭；局部重试恢复成功。观点阅读卡同步显示 44 px 本地占位和就地重试，不影响其余 30 张观点卡。

### F-011 主题人格库

**状态：已完成；待发布**

**用户故事**

1. 作为访客，我希望人格名称贴合当前讨论主题，而不是所有问题都显示同一套通用九派。
2. 作为内容维护者，我希望人物、文案和素材有稳定契约及审核边界，可以逐主题扩展。
3. 作为旧链接访问者，我希望原有 `cast` 参数继续有效，不因人物名称变化而失效。

**验收标准**

- 目录定义八个一级主题，只有恰好包含九位已验收人物的主题可进入运行时。
- “人生选择、生活方式与精神世界”和“工作、科技与未来”主题各包含九位互补且可区分的人物原型。
- `career-35` 绑定人生选择主题；`ai-math` 与 `scholars-ai-math` 绑定工作科技主题；
  主题缺失、未知、未完成或模块加载失败时继续使用原有九派。
- `fox/bear/cat/owl/rabbit/penguin/redpanda/goat/frog` 继续作为稳定槽位，
  分享 URL、人格计算和本地存储格式保持向后兼容。
- React 人格卡、朋友对照和 Three.js 人格卡群消费同一主题人格数据源。
- 每位人物记录名称、角色描述、适用信号、选择理由、素材来源与使用边界。
- 不选择中国近代及以后人物；人物原型只解释当前问题表达，不描述用户的长期人格。
- 两个主题复用的九张抽象插画均通过格式、尺寸、文件大小和本地路径检查。

**验证记录**

- 2026-09-14：`npm run build` 通过 TypeScript、导航、分享对照、同频发现、身份修订、
  星云 LOD、主题人格契约和 Vite 生产构建检查；契约确认八个主题、九个稳定槽位、
  九张 512 × 512 本地 JPEG 及经典九派降级。
- 2026-09-14：真实浏览器在 1280 × 900 下确认 `career-35` 的 React 人格卡、
  Three.js 人格卡群和 3D 人格书显示同一套九位人物；无主题和未知主题保留经典九派，
  阻断人格库模块时也可降级并在恢复后重新加载。
- 2026-09-14：真实浏览器确认人格卡分享 URL 继续使用稳定 `cast=fox` 路由槽位；
  390 × 844 下最长名称“弗吉尼亚·伍尔夫”、角色和描述均不溢出，两枚主要按钮为
  48 px，React 页面及书架 iframe 均无横向溢出。
- 2026-09-14：`ai-math` 与 `scholars-ai-math` 的人格筛选均显示图灵、笛卡尔、庞加莱、
  哥德尔、希尔伯特、欧几里得、莱布尼茨、拉卡托斯和拉马努金；真实浏览器确认
  `scholars-ai-math/fox` 的 3D 人格书为图灵，`ai-math/owl` 为哥德尔，WebGL 画面非空。

### F-012 品牌落地页

**状态：已完成；待发布**

**用户故事**

1. 作为首次访问者，我希望先理解思想银河解决什么问题，再进入产品体验。
2. 作为移动端访问者，我希望在首屏看清品牌、价值和进入动作。

**验收标准**

- `/landing` 使用全幅 Structure Flow 粒子场展示“思想银河”品牌，并露出下一段内容。
- 页面末尾使用独立全屏收尾段，通过 Particle Wordmark 动画显示 `Soular`；首屏不加载
  该字标。
- 收尾段提供正式 GitHub 仓库入口和进入思想银河的主操作。
- 主操作进入现有 `/` 产品流程，不替换首页，也不改变现有路由行为。
- 页面说明观点结构、自我定位和同频发现，不展示虚构用户或虚构业务数据。
- `StructureFlowCollection` 使用 ThreeUI 注册源码中的 `structure-flow` 变体。
- 参数固定为 `speed=1.00`、`pointSize=0.080`、`opacity=0.40`、
  `maskStart=0.20` 和 `maskSolid=0.50`。
- `TextAnimationCollection` 使用 `particle-wordmark` 变体，参数固定为 `mode=dark`、
  `hue=0`、`saturation=1.00` 和 `brightness=1.00`。
- `Soular` 字样、字号和页面底色仅在 Vite 构建期间替换，不修改供应方注册文件。
- Structure Flow 的五个注册文件和 Particle Wordmark 的三个注册文件均保持供应方
  内容与 SHA-256 不变。
- 系统开启减少动态效果时，首屏不启动 Structure Flow 动画；动画模块加载或渲染失败时，
  页面保留静态深色背景与完整内容。
- 桌面端与 390 px 移动端画布非空，页面无横向溢出，进入动作可用。

**验证记录**

- 2026-09-18：五个 ThreeUI 注册文件的 SHA-256 与来源清单一致。
- 2026-09-18：`npm run build` 通过 TypeScript、六组产品契约检查和 Vite 生产构建。
- 2026-09-18：Playwright 在 1280 × 900 与 390 × 844 下确认粒子画布非空，
  页面无横向溢出，下一段内容在首屏露出 52 px 和 44 px；主操作从 `/landing`
  进入 `/`。
- 2026-09-18：Particle Wordmark 三个注册文件的 SHA-256 与来源清单一致；
  1280 × 900 和 390 × 844 下的独立收尾段均显示动态 `Soular` 粒子字标，
  GitHub 与产品入口可用，控制台无错误且页面无横向溢出。

### F-013 知乎 × 思想银河开场动画

**状态：已完成；待发布**

**用户故事**

1. 作为首次访问者，我希望在进入首页前快速理解“知乎讨论如何变成观点星云”。
2. 作为重复访问者，我希望开场不会在同一会话中反复打断探索。

**验收标准**

- 开场仅在公开首页 `/` 首次进入时显示；`/landing`、受保护深链、问题确认与探索回跳
  不得被拦截。
- 视觉叙事依次呈现知乎、思想银河、双方联名、观点星云和最终品牌，总时长不超过
  9 秒，不请求知乎或 AI 接口。
- 画布从真实字体轮廓采样坐标，粒子必须先聚合成“知乎”，再逐点重排成“思想银河”，
  第三幕继续重排成“知乎 × 思想银河”，随后从联名字形解体为双旋臂星云；三组粒子字
  在 390 px 与桌面视口均可辨认。
- 开场默认无声音，提供“跳过开场”和“进入思想银河”两个明确动作，并支持 `Esc` 退出。
- 完成或跳过后写入带版本的 Session Storage 标记；同一标签页会话内不再自动播放。
- 使用 `?intro=1` 可以显式重播，便于演示和验收。
- 系统开启减少动态效果时直接进入首页，不启动粒子画布。
- 动画期间首页内容不可聚焦或被辅助技术读取；退出后恢复原有登录门槛与路由行为。
- 画布仅使用本地代码与现有品牌标志，桌面端和 390 px 移动端无横向溢出，触控按钮
  高度不小于 44 px。

**验证记录**

- 2026-09-18：`npm run build` 通过 TypeScript、七组产品契约检查和 Vite 生产构建。
- 2026-09-18：Playwright 在 1280 × 900 下确认开场粒子画布非空，四阶段顺序完成，
  最终进入按钮可用；按 `Esc` 后首页挂载且会话标记写入，再次进入 `/` 不重播。
- 2026-09-18：390 × 844 下无横向溢出，跳过与进入按钮均完整位于视口内且高度不小于
  44 px；减少动态模式、`/landing` 和 `/?confirm=1` 均不挂载开场。
- 2026-09-18：开场独立运行期间控制台无错误；首页仅在开场退出时挂载，避免后台提前
  初始化人格卡 iframe 与星云资源。
- 2026-09-18：1280 × 900 与 390 × 844 实机截图确认粒子可完整拼出“知乎”、
  “思想银河”和“知乎 × 思想银河”，说明文字位于字形下方且页面无横向溢出。
- 2026-09-18：从 `/landing` 站内进入首页会启动首次开场，站内打开 `?intro=1` 可重播；
  844 × 390 横屏下最终操作与底部进度互不遮挡。
- 2026-09-18：开场粒子字、粒子密度和最终标题会按视口宽高共同缩放；大屏增强主体占比，
  超宽屏以短边为准并限制倍率，移动端和 844 × 390 横屏布局保持原尺寸。
- 2026-09-19：最终品牌幕增加刘看山动态入场，由左向右平滑推动“思想银河”标题；角色与
  标题共用减速轨迹，操作按钮随后出现，整段仍包含在原 8.4 秒开场时长内。

## 6. 测试决策

- 用户可见行为优先通过页面和 HTTP 接口验证，不测试私有实现细节。
- Three.js 场景必须检查画布像素非空、关键控件位置和桌面/移动端布局。
- 路由、iframe 消息和人格卡参数使用现有导航检查脚本。
- 服务端变更运行 TypeScript 检查；缓存、认证或接口契约变化同步更新接口文档。
- 动态内容测试优先使用固定响应，避免验证过程消耗知乎或模型配额。

## 7. 当前范围之外

- 每位用户即时生成一片独立观点星云。
- 未经人工验收直接发布模型分析结果。
- 无限制开放式 AI 聊天。
- 批量抓取知乎全文或绕过开放平台限制。
- 将一次讨论中的人格结果描述为永久心理画像。

## 8. 规格维护规则

每项功能在完成前必须更新本文件：

1. 明确问题、用户故事、行为和范围边界。
2. 写出可观察、可复现的验收标准。
3. 更新功能状态和发布状态。
4. 写入实际执行的验证记录。
5. 接口、字段、错误、缓存、认证或跨域行为变化时同步更新 `docs/api-contract.md`。
6. 对外能力、启动方式、架构、目录或授权边界变化时同步更新 `README.md`。
7. 长期协作规则变化时同步更新 `AGENTS.md`。

缺少上述记录的功能不视为完成。
