# 思想银河 · 星云主体交互改造方案

> 设计依据： [思想银河 PRD](https://my.feishu.cn/wiki/VLgfw0mBgiKEVcks5MocswBZnDe)
>
> 本文是基于当前实现的落地方案，不修改 PRD。产品不再向知乎信息流形态扩展，唯一主舞台是观点星云。

## 1. 方案结论

核心体验固定为：

```text
漂浮问题场 → 中央确认 → 原地汇聚生成 → 星云探索 → 3 次表态解锁人格 → 同频发现 / 分享
```

- 首页直接进入星云世界，不再以人格卡轮播作为主页面。
- 生成过程只揭示已经发布、人工验收过的静态快照，不为访客实时请求知乎回答或调用 AI。
- 搜索和推荐只查本地已发布快照；热榜仅在用户明确打开时调用知乎接口。
- 头像、观点卡、人格和社交关系都依附于星云，不新增知乎式推荐流或独立社区信息流。
- 首期复用现有实现，不新增依赖，不重写 Three.js 引擎。

## 2. 当前实现与目标差距

| 项目 | 当前实现 | 目标 |
| --- | --- | --- |
| 首页 | `/` 是人格卡波浪，星云是次级入口 | `/` 以问题场和星云为主体，人格卡是探索结果 |
| 选题 | 进入 `/nebula` 后在工具盒内切换 | 先看到 6 个漂浮问题，选择后在中央确认 |
| 生成 | 只有资源加载提示，完成星云直接出现 | 按 PRD 在同一画布内经历汇聚、分离、成带、头像浮现 |
| 探索 | 旋转、缩放、观点卡、阅读模式已经可用 | 保留能力，但弱化列表模式，优先在星云内完成探索 |
| 人格 | 0 次点赞也能使用默认人格抽卡 | 至少完成 3 次有效点赞后解锁 |
| 社交发现 | 已有对立观点匹配；小圈子依赖快照预设 | 3 次表态后开放；无圈子数据时不展示空入口 |
| 内容规模 | 仅 2 个快照，其中 1 个为 mock | 首屏至少 6 个已发布候选，优先补齐真实快照 |
| 观点坐标 | 两极标签固定在 HUD | 两极标签锚定星云端点，随星云旋转与缩放 |
| 推荐 | UI 显示“正在开发中” | 从本地快照按主题标签推荐，不调用推荐接口 |
| 热榜 | 用户打开时请求，但 10 分钟缓存可能超额 | 单一缓存键、服务端切片、长缓存与旧数据降级 |

## 3. 页面状态机

页面只维护五个互斥主状态，悬浮观点卡、发现卡和分享卡属于主状态上的临时浮层。

| 状态 | 用户看到什么 | 可执行动作 | 进入下一状态 |
| --- | --- | --- | --- |
| `DISCOVER` | 星空背景、中央引导、周边 6 个问题碎片 | 浏览、搜索本地快照、主动打开热榜、选择问题 | 选中问题 → `CONFIRM` |
| `CONFIRM` | 中央问题详情、回答数和“开始生成”按钮；周边卡片降至 30% | 切换问题、返回发现、开始生成 | 点击生成 → `GENERATING` |
| `GENERATING` | 光点汇聚、观点分离、光谱成形、头像浮现 | 跳过动画；减少动态效果用户直接完成 | 动画完成或跳过 → `EXPLORE` |
| `EXPLORE` | 完整星云、观点节点、自己的星位和探索工具 | 旋转、缩放、读观点、点赞、搜索本地快照 | 有效点赞达到 3 次 → `PERSONA_UNLOCKED` |
| `PERSONA_UNLOCKED` | 星位已形成，人格入口和同频发现被点亮 | 生成人格卡、找同频/对立观点、分享 | 取消点赞至不足 3 次 → `EXPLORE` |

状态切换必须由单一状态值控制，避免通过多个 CSS class 推断当前页面。切换快照时回到该快照的 `CONFIRM`；已经看过生成动画的快照可在本次会话提供“直接进入星云”。

## 4. 核心交互逻辑

### 4.1 `DISCOVER`：漂浮问题场

- 中央保留 PRD 的主标题、简短说明和本地搜索入口。
- 周边固定显示 6 个已发布快照，位置经过设计，不在每次加载时随机重排。
- 卡片展示问题标题、代表观点短句和回答规模；悬停或键盘聚焦时轻微上浮并增强辉光。
- 搜索只匹配本地快照的标题、标签和两极文案；没有结果时提供知乎搜索链接，但不承诺现场生成星云。
- “热榜”不是默认展开项。用户明确点击后才请求；未制作的热榜问题只能查看知乎原文或进入候选池。
- 移动端改为围绕中央上下分布的 3～4 张卡片，并提供“查看更多”，不强行塞满 6 张。

### 4.2 `CONFIRM`：中央确认

- 用户选择任意漂浮卡片、本地搜索结果或已发布热榜项后，中央内容在 200ms 内淡出，周边卡片同步降至 30% 透明度。
- 延迟 200ms 后，问题标题、两极观点、样本数量和“开始生成思想银河”在 300ms 内淡入，总时长约 500ms。
- 周边卡片仍可点击；切换选择时只替换中央内容，不退回发现状态。
- “重新选择”回到 `DISCOVER`。未制作的热榜条目不出现生成按钮，只显示“尚未制作”与知乎原文入口。

### 4.3 `GENERATING`：原地汇聚生成

生成不是服务端计算进度，而是对已加载静态快照的叙事性揭示。动画期间不发起知乎或 AI 请求。

| 时间 | 画面 | 文案 |
| --- | --- | --- |
| 0–1s | 问题确认层淡出，保留同一星空与相机 | 正在展开该问题的观点样本 |
| 1–4s | 快照节点从四周沿曲线飞向中心 | 正在汇聚 N 个代表观点 |
| 4–6s | 节点按已保存的立场坐标向两侧分离 | 观点正在分布到银河 |
| 6–8s | 浅 S 型光谱由后层、主体层、前层粒子形成 | 显示两端立场标签 |
| 8–12s | 头像按代表性权重分批浮现，光谱前层粒子继续包裹头像 | 不显示伪造的实时 AI 百分比 |
| 12s 后 | 控件和自己的星位淡入，星云缓慢自动旋转 | 点击观点 · 拖拽旋转 · 点赞形成你的星位 |

- 使用现有节点最终坐标作为动画终点，不建立第二套布局算法。
- 动画随机性只影响轨迹、延迟和微小波动，不能改变观点坐标或人格结果。
- 资源加载失败或用户点击“跳过”时直接进入完整星云；`prefers-reduced-motion` 默认跳过长动画。
- 不切换到新的视觉页面，避免路由跳转和白屏破坏“星云正在眼前生成”的连续感。

### 4.4 视觉设计约束：光谱包容头像

- **连续实时感**：汇聚、分离、成带和头像浮现是同一段连续运动，不做成多个互相切断的加载步骤。
- **物理真实感**：光点轨迹具有加速、惯性和缓冲；汇聚像被引力吸引，分离像被磁场推开，头像出现带轻微弹性。
- **有机随机感**：每次只随机飞入轨迹、头像出现顺序和光谱呼吸，不随机最终坐标、立场或人格结果。
- **层次与深度**：场景至少由背景星点 → 光谱后层粒子 → 头像 → 光谱前层粒子四层构成；头像暖边框突出，但不能遮住光谱主体。
- **光谱包容头像**：头像位于浅 S 型光谱内部，前后都有粒子流过，表现为被星云环抱和承载，禁止做成贴在光谱表面的头像墙。
- **氛围优先**：整体运动舒缓、优雅，让用户感到好奇和沉浸；不使用高频闪烁、急促进度条或制造等待焦虑的倒计时。

这条关系同时表达信息层级：光谱是整个问题的观点场，头像只是场中的个体样本。两极标签必须锚定光谱端点并随场景运动，不能成为与星云分离的 HUD 装饰。

### 4.5 `EXPLORE`：星云探索

- 初始相机能看见完整浅 S 光谱和主要头像，两极标签作为 3D 锚点跟随星云。
- 自动旋转保持缓慢；用户拖拽后暂停，空闲数秒再恢复。滚轮、触控捏合控制缩放，限制最近和最远距离。
- 鼠标悬停头像 300ms 显示轻量观点摘要；点击则固定卡片。任意时刻只允许一张观点卡处于固定状态。
- 观点卡保留作者、人格标签、摘要、立场位置和知乎原文入口；点赞是站内“我认同这个观点”的本地表态，不伪装为同步知乎赞同。
- “光谱阅读”只作为辅助可访问视图，用于搜索、筛选和连续阅读；入口降级为次级工具，不再成为产品主形态。
- “随机发现”移动相机到一个未读节点，先定位再打开卡片；不直接弹出列表。
- `Escape` 的关闭顺序：固定观点卡 → 功能浮层 → 聚焦状态 → 工具盒。

### 4.6 三次表态与人格解锁

- 只统计对回答节点的不同点赞；评论区空心节点不计入。
- 0 次：自己的星位保持待形成状态，不显示默认人格结果。
- 1～2 次：实时移动星位，并显示“再表态 X 次解锁人格”。
- 第 3 次：自己的星位完成一次短促聚光动画，人格入口与同频发现同时解锁。
- 3 次及以上：按现有稳定算法计算平均立场和九派人格；继续点赞会更新位置与解释。
- 取消点赞后不足 3 次时重新锁定入口，但已分享的人格链接仍可独立打开。
- 人格结果必须表述为“基于本次问题中的点赞”，不能描述为永久人格或心理诊断。

### 4.7 同频发现与分享

- “观点碰撞”继续从当前快照中选择立场距离较远且未点赞的观点。
- “同频发现”使用快照内预生成的圈子数据和当前点赞坐标匹配，不在页面运行时调用 AI。
- 当前快照没有合格圈子时不自动弹出发现卡，也不展示无效按钮。
- 聚焦圈子时保留光谱，其他头像淡出但不消失，强调“群体在整体讨论中的位置”。
- 分享优先生成带问题、星位、人格和返回星云链接的卡片；不新增社区动态流。

## 5. 知乎接口额度方案

开放平台默认按租户、按能力组计算自然日额度：常见为每组 100 次，未实名或低额度账号可能只有 10 次；实际值以官方 `quota` 返回为准，查询额度本身不消耗业务额度。

### 5.1 在线访问

| 行为 | 知乎 / AI 调用 | 规则 |
| --- | --- | --- |
| 打开首页、生成动画、探索、点赞、人格、匹配、分享 | 0 | 全部读取已发布快照并本地计算 |
| 搜索问题 | 0 | 只查本地快照；无结果提供知乎直达 |
| 查看推荐 | 0 | 按本地标签和相似度推荐已发布快照 |
| 打开热榜 | 最多 1 次缓存回源 | 必须由用户明确点击，不预加载 |
| OAuth 状态 | 0 次内容接口 | 只读已有会话；不自动拉取完整画像 |
| 个性化画像 | 按需 | 登录用户明确触发，单账号长缓存并显示额度失败降级 |

热榜实现必须改为：

1. 服务端始终以 `Limit=30` 请求一次，并使用唯一缓存键；前端的 `limit` 只对缓存结果切片，不能制造多个上游缓存键。
2. 已确认账号有 100 次日额度时，缓存至少 1 小时，持续流量理论上最多回源 24 次/日。
3. 未确认额度或实际只有 10 次时，缓存至少 6 小时，最多回源 4 次/日。
4. 上游失败优先返回最后一次成功结果；没有旧数据时展示本地已发布话题和知乎热榜直达，不连续重试。
5. 达到日限额或业务限流后立即熔断到次日，不把用户刷新转成新的上游请求。

### 5.2 离线快照生产

默认一次制作 3 个候选话题，最坏预算为：

- 热榜：1 次。
- 问题回答：最多 9 次（每题最多 3 页）。
- 知乎直答：3 次（每题 1 次批量分析）。

执行规则：

- 仅由运营人员手动运行，开始前查询对应能力组剩余额度。
- 为每组保留至少 20% 日额度；余额不足时缩小批次或停止，不自动跨天重跑。
- 配额、鉴权或频率错误立即停止整批；不重复请求失败候选，也不覆盖已有产物。
- 生成结果先进入候选文件，人工检查来源、摘要、坐标、两极文案、头像和圈子后再发布。
- 线上不得暴露回答采集、光谱生成或直答代理接口。

## 6. 最小实施顺序

### P0：先闭合主体验

1. 让 `/` 进入星云主体流程，原人格波浪不再占据首页；`/nebula` 保留为兼容入口。
2. 在现有 `public/nebula-scene/index.html` 内加入五态状态机和问题场，复用当前画布、快照与控件。
3. 将现有资源加载过渡改成快照揭示动画；不改观点坐标算法。
4. 增加 3 次点赞门槛，移除 0 次点赞的默认人格抽取。
5. 把两极标签绑定到星云端点，并补齐生成跳过、失败降级与减少动态效果。

### P1：补内容与额度保护

1. 增加至少 4 个经过验收的真实快照，使问题场达到 6 个可选问题。
2. 为真实快照补齐圈子数据；没有数据时隐藏入口。
3. 推荐改为本地快照推荐，热榜改成规范化单键缓存与服务端切片。
4. 修正文档中“推荐已完成”等与真实 UI 不一致的状态。

### P2：再增强个性化

1. 登录用户明确授权后，将公开头像用于“我”的节点。
2. 只有用户主动点击时才读取画像并提供个性化起始建议。
3. 在实际留存和额度数据证明有价值前，不做实时 AI 导览和用户专属星云。

## 7. 文件落点

- `src/App.tsx`、`src/Home.tsx`：将首页入口切到星云主体，保留旧路径兼容。
- `src/Nebula.tsx`、`src/NebulaStage.tsx`：传递入口状态并继续承接 iframe 导航与用户公开头像。
- `public/nebula-scene/index.html`：状态机、问题场、生成动画、三次点赞门槛和星云内交互。
- `public/nebula-scene/presets.js`、`preset-*.js`：已发布问题目录、快照元数据与圈子数据。
- `server/src/zhihu/client.ts`、`server/src/core/app.ts`：热榜单键缓存、TTL、切片和熔断降级。
- `spec.md`：实现时同步功能状态、验收标准和真实验证记录。
- `docs/api-contract.md`：仅在热榜接口缓存或响应契约实际变化时更新。

不引入新的状态库、动画库、推荐服务或运行时 AI 接口。

## 8. 验收标准

- 首次访问无需理解人格卡，15 秒内能完成“选择问题 → 看到星云 → 打开一个观点”。
- 从问题确认到完整星云没有页面白屏，生成期间网络面板没有知乎回答或 AI 请求。
- 生成动画结束后，旋转、缩放、观点卡、原文链接和快照切换均可用。
- 前 2 次点赞不能打开我的人格卡；第 3 次立即解锁，刷新后按当前快照版本正确恢复。
- 搜索和推荐在断网时仍能打开已发布快照；热榜失败时仍有本地内容和知乎直达入口。
- 未主动打开热榜时，页面生命周期内不调用热榜接口；不同前端 `limit` 不产生不同上游缓存键。
- 桌面端与 390px 移动端无横向溢出，触控目标至少 44px；减少动态效果下可直接进入稳定星云。
- `npm run build`、`npm run check:navigation`、`npm run server:check` 和 `git diff --check` 通过。
